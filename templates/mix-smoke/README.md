# mix-smoke

Positional mix DataChannel commands for voice-data-mix-smoke (Voice+Data, shared child). Not seeded on project create.

## Build

```bash
npx @voicethere/agent build --entry templates/mix-smoke/agent.ts --outfile dist/agent.js
```

## Sources

- `agent.ts`
