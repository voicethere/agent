# echo-smoke

Minimal echo agent for e2e voice-smoke, agent-smoke, and cli-smoke uploads. Not seeded on project create.

## Build

```bash
npx @voicethere/agent build --entry templates/echo-smoke/agent.ts --outfile dist/agent.js
```

## Sources

- `agent.ts`
