# crash

Crash / echo agent for session-errors-smoke and agent-crash-policy smokes. Not seeded on project create.

## Build

```bash
npx @voicethere/agent build --entry templates/crash/agent.ts --outfile dist/agent.js
```

## Sources

- `agent.ts`
