# game-sync-smoke

Minimal data-channel agent for deploy-smoke, shared-child, and idle smokes. Not seeded on project create.

## Build

```bash
npx @voicethere/agent build --entry templates/game-sync-smoke/agent.ts --outfile dist/agent.js
```

## Sources

- `agent.ts`
