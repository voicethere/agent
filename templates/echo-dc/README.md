# echo-dc

Data-channel-only echo — relays speech events and chat text over DC without TTS.

## Build

```bash
npx @voicethere/agent build --entry templates/echo-dc/agent.ts --outfile dist/agent.js
```

## Sources

- `agent.ts`
