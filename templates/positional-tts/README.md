# positional-tts

Voice+Data demo — enables positional mixing and orbits each listener’s TTS speaker with `setTtsPose`. Requires Voice+Data runner mode.

## Build

```bash
npx @voicethere/agent build --entry templates/positional-tts/agent.ts --outfile dist/agent.js
```

## Sources

- `agent.ts`, `orbit.ts`
