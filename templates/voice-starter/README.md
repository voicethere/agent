# voice-starter

Starter bundle covering every speech event from `@node-webrtc-rust/sdk/voice`. Customize `onUserSpeechFinal` for your LLM or tools.

## Build

```bash
npx @voicethere/agent build --entry templates/voice-starter/agent.ts --outfile dist/agent.js
```

## Sources

- `agent.ts`
